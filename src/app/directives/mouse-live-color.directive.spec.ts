import { MouseLiveColorDirective } from './mouse-live-color.directive';

describe('MouseLiveColorDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseLiveColorDirective();
    expect(directive).toBeTruthy();
  });
});
